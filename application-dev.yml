spring:
  application:
    name: dev

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/analysis
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

# dynamodb
aws:
  dynamodb:
    endpoint: http://localhost:8000
  region: ap-northeast-2
  accessKey: accesskey
  secretKey: secretKey